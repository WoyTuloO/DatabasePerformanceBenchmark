@UtilityClass
public final class ShopRequests {

    // ═══════════════════════════ CREATE ═══════════════════════════

    /** C1 – Dodanie zamówienia */
    public record CreateOrder(
            long customerId,
            String shippingCountry,
            String shippingCity,
            String shippingPostalCode,
            String shippingStreet,
            String shippingBuildingNo,
            String shippingApartmentNo,
            String currency
    ) {}

    /** C2 – Dodanie itemu do koszyka */
    public record AddOrderItem(
            long orderId,
            long productId,
            int quantity,
            int unitPriceCents
    ) {}

    /** C3 – Dodanie klienta */
    public record CreateCustomer(
            String email,
            String passwordHash,
            String firstName,
            String lastName,
            String phone
    ) {}

    /** C4 – Dodanie produktu */
    public record CreateProduct(
            String stockKeepingUnit,
            String name,
            String description,
            Long brandId,
            Long categoryId,
            int basePriceCents,
            String currency
    ) {}

    /** C5 – Upsert stanu magazynowego */
    public record UpsertInventory(
            long warehouseId,
            long productId,
            int quantity
    ) {}

    /** C6 – Dodanie płatności do zamówienia */
    public record CreateOrderPayment(
            long orderId,
            long paymentMethodId,
            String provider,
            int amountCents,
            String currency,
            String status,
            OffsetDateTime paidAt
    ) {}

    // ═══════════════════════════ UPDATE (body) ═══════════════════════════

    /** U1 / U4 – Mnożnik ceny */
    public record PriceMultiplier(double multiplier) {}

    /** U2 – Zmiana statusu zamówienia */
    public record UpdateStatus(String status) {}

    /** U3 – Zmiana aktywności produktu */
    public record UpdateActive(boolean active) {}

    /** U5 – Zmiana ilości (quantity) */
    public record UpdateQuantity(int quantity) {}
}



---------------------------



@RestController
@RequestMapping("/api/shop")
public class ShopCrudController {

    private final ShopService shopService;

    public ShopCrudController(ShopService shopService) {
        this.shopService = shopService;
    }

    private Map<String, Object> affectedRows(int count) {
        return Map.of("affectedRows", count);
    }

    // ═══════════════════════════════════════════════════════════════════
    //  CREATE
    // ═══════════════════════════════════════════════════════════════════

    @Operation(
            summary = "C1 – Utwórz zamówienie",
            description = "Tworzy nowy nagłówek zamówienia (koszyk) ze statusem NEW, total_cents = 0 "
                    + "i podanym adresem dostawy. created_at ustawiany automatycznie. Zwraca order_id."
    )
    @PostMapping("/orders")
    public ResponseEntity<Map<String, Object>> createOrder(
            @RequestBody ShopRequests.CreateOrder req) {
        return ResponseEntity.ok(shopService.createOrder(req));
    }

    @Operation(
            summary = "C2 – Dodaj pozycję do koszyka",
            description = "Dodaje nową pozycję (order_item) do istniejącego zamówienia. "
                    + "line_no jest automatycznie wyliczany jako MAX(line_no)+1 z podzapytania (unika race condition). "
                    + "Zwraca order_id i line_no."
    )
    @PostMapping("/order-items")
    public ResponseEntity<Map<String, Object>> addOrderItem(
            @RequestBody ShopRequests.AddOrderItem req) {
        return ResponseEntity.ok(shopService.addOrderItem(req));
    }

    @Operation(
            summary = "C3 – Utwórz klienta",
            description = "Rejestruje nowego klienta. Email musi być unikalny (UNIQUE constraint). "
                    + "created_at ustawiany automatycznie. Zwraca customer_id."
    )
    @PostMapping("/customers")
    public ResponseEntity<Map<String, Object>> createCustomer(
            @RequestBody ShopRequests.CreateCustomer req) {
        return ResponseEntity.ok(shopService.createCustomer(req));
    }

    @Operation(
            summary = "C4 – Utwórz produkt",
            description = "Dodaje nowy produkt z ceną bazową, walutą, marką i kategorią. "
                    + "Produkt domyślnie aktywny (active = TRUE), created_at automatycznie. Zwraca product_id."
    )
    @PostMapping("/products")
    public ResponseEntity<Map<String, Object>> createProduct(
            @RequestBody ShopRequests.CreateProduct req) {
        return ResponseEntity.ok(shopService.createProduct(req));
    }

    @Operation(
            summary = "C5 – Upsert stanu magazynowego",
            description = "Wstawia lub aktualizuje stan magazynowy dla pary (warehouse_id, product_id). "
                    + "Wykorzystuje PostgreSQL ON CONFLICT — jeśli rekord istnieje, nadpisuje quantity i updated_at. "
                    + "Zwraca warehouse_id, product_id, quantity."
    )
    @PutMapping("/inventory")
    public ResponseEntity<Map<String, Object>> upsertInventory(
            @RequestBody ShopRequests.UpsertInventory req) {
        return ResponseEntity.ok(shopService.upsertInventory(req));
    }

    @Operation(
            summary = "C6 – Dodaj płatność do zamówienia",
            description = "Rejestruje płatność (order_payment) powiązaną z zamówieniem i metodą płatności. "
                    + "Dozwolone statusy: PENDING, AUTHORIZED, CAPTURED, FAILED, REFUNDED. "
                    + "created_at automatycznie. Zwraca order_payment_id."
    )
    @PostMapping("/order-payments")
    public ResponseEntity<Map<String, Object>> createOrderPayment(
            @RequestBody ShopRequests.CreateOrderPayment req) {
        return ResponseEntity.ok(shopService.createOrderPayment(req));
    }

    // ═══════════════════════════════════════════════════════════════════
    //  READ
    // ═══════════════════════════════════════════════════════════════════

    @Operation(
            summary = "R1 – Brakujące produkty w zamówieniu",
            description = "Wyszukuje pozycje zamówienia, których łączny stan na magazynach (total_stock) "
                    + "jest mniejszy niż zamówiona ilość (ordered_quantity). "
                    + "Zwraca: line_no, product_id, stock_keeping_unit, name, ordered_quantity, total_stock."
    )
    @GetMapping("/orders/{orderId}/missing-products")
    public ResponseEntity<List<Map<String, Object>>> getMissingProducts(
            @PathVariable long orderId) {
        return ResponseEntity.ok(shopService.getMissingProducts(orderId));
    }

    @Operation(
            summary = "R2 – Dostępne produkty marki w mieście",
            description = "Zwraca DISTINCT listę aktywnych produktów danej marki, które mają quantity > 0 "
                    + "w magazynach znajdujących się w podanym mieście. "
                    + "Zwraca: product_id, stock_keeping_unit, name, base_price_cents, currency, active."
    )
    @GetMapping("/products/available")
    public ResponseEntity<List<Map<String, Object>>> getAvailableProducts(
            @Parameter(description = "Nazwa marki (brands.name)") @RequestParam String brandName,
            @Parameter(description = "Miasto magazynu (warehouses.city)") @RequestParam String city) {
        return ResponseEntity.ok(shopService.getAvailableProductsByBrandAndCity(brandName, city));
    }

    @Operation(
            summary = "R3 – Zawartość koszyka",
            description = "Zwraca pozycje zamówienia (order_items) z danymi produktów. "
                    + "Kalkuluje line_total_cents = quantity × unit_price_cents. Posortowane wg line_no."
    )
    @GetMapping("/orders/{orderId}/items")
    public ResponseEntity<List<Map<String, Object>>> getCartItems(
            @PathVariable long orderId) {
        return ResponseEntity.ok(shopService.getCartItems(orderId));
    }

    @Operation(
            summary = "R4 – Dostępność produktu (Total Stock)",
            description = "Zwraca dane produktu wraz z sumarycznym stanem magazynowym (total_stock) "
                    + "ze wszystkich magazynów (LEFT JOIN inventory). "
                    + "Zwraca: product_id, stock_keeping_unit, name, active, total_stock."
    )
    @GetMapping("/products/{productId}/availability")
    public ResponseEntity<Map<String, Object>> getProductAvailability(
            @PathVariable long productId) {
        return ResponseEntity.ok(shopService.getProductAvailability(productId));
    }

    @Operation(
            summary = "R5 – Maile klientów płacących daną metodą",
            description = "Zwraca DISTINCT emaile klientów, którzy mają zamówienia opłacone podaną metodą płatności "
                    + "(np. BLIK, CARD). JOIN: order_payments → orders → customers → payment_methods."
    )
    @GetMapping("/customers/by-payment-method/{code}/emails")
    public ResponseEntity<List<Map<String, Object>>> getCustomerEmailsByPaymentMethod(
            @Parameter(description = "Kod metody płatności (payment_methods.code), np. BLIK") @PathVariable String code) {
        return ResponseEntity.ok(shopService.getCustomerEmailsByPaymentMethod(code));
    }

    @Operation(
            summary = "R6 – Szczegóły zamówień klienta (pełna historia)",
            description = "Zwraca pełne szczegóły zamówień klienta: dane zamówienia, pozycje z produktami "
                    + "(+ marka, kategoria) oraz płatności z metodą płatności. JOIN na 7 tabelach. "
                    + "LEFT JOIN dla brands, categories, order_payments, payment_methods."
    )
    @GetMapping("/customers/{customerId}/order-details")
    public ResponseEntity<List<Map<String, Object>>> getCustomerOrderDetails(
            @PathVariable long customerId) {
        return ResponseEntity.ok(shopService.getCustomerOrderDetails(customerId));
    }

    // ═══════════════════════════════════════════════════════════════════
    //  UPDATE
    // ═══════════════════════════════════════════════════════════════════

    @Operation(
            summary = "U1 – Mnożnik ceny dla kategorii",
            description = "Mnoży base_price_cents wszystkich produktów w danej kategorii przez podany mnożnik. "
                    + "GREATEST(0, ...) zabezpiecza przed ceną poniżej 0. "
                    + "CAST(... AS integer) zabezpiecza typ kolumny. Zwraca liczbę zmienionych wierszy."
    )
    @PatchMapping("/categories/{categoryId}/prices")
    public ResponseEntity<Map<String, Object>> updateCategoryPrices(
            @PathVariable long categoryId,
            @RequestBody ShopRequests.PriceMultiplier req) {
        return ResponseEntity.ok(affectedRows(shopService.updateCategoryPrices(categoryId, req.multiplier())));
    }

    @Operation(
            summary = "U2 – Zmień status zamówienia na podstawie payment_id",
            description = "Ustawia nowy status zamówienia powiązanego z podaną płatnością (order_payment_id). "
                    + "Używa rozszerzenia PostgreSQL (klauzula FROM w UPDATE). "
                    + "Dozwolone statusy: NEW, PAID, SHIPPED, CANCELLED, RETURNED. Zwraca liczbę zmienionych wierszy."
    )
    @PatchMapping("/order-payments/{orderPaymentId}/order-status")
    public ResponseEntity<Map<String, Object>> updateOrderStatusByPayment(
            @PathVariable long orderPaymentId,
            @RequestBody ShopRequests.UpdateStatus req) {
        return ResponseEntity.ok(affectedRows(shopService.updateOrderStatusByPayment(orderPaymentId, req.status())));
    }

    @Operation(
            summary = "U3 – Włącz/wyłącz produkt",
            description = "Ustawia flagę active produktu na TRUE lub FALSE (soft-disable). "
                    + "Zwraca liczbę zmienionych wierszy (0 = nie znaleziono, 1 = zaktualizowano)."
    )
    @PatchMapping("/products/{productId}/active")
    public ResponseEntity<Map<String, Object>> updateProductActive(
            @PathVariable long productId,
            @RequestBody ShopRequests.UpdateActive req) {
        return ResponseEntity.ok(affectedRows(shopService.updateProductActive(productId, req.active())));
    }

    @Operation(
            summary = "U4 – Mnożnik ceny dla marki",
            description = "Mnoży base_price_cents wszystkich produktów danej marki przez podany mnożnik. "
                    + "CAST(... AS integer) zachowuje typ kolumny. "
                    + "UWAGA: brak GREATEST — cena może spaść poniżej 0 przy ujemnym mnożniku. "
                    + "Zwraca liczbę zmienionych wierszy."
    )
    @PatchMapping("/brands/{brandId}/prices")
    public ResponseEntity<Map<String, Object>> updateBrandPrices(
            @PathVariable long brandId,
            @RequestBody ShopRequests.PriceMultiplier req) {
        return ResponseEntity.ok(affectedRows(shopService.updateBrandPrices(brandId, req.multiplier())));
    }

    @Operation(
            summary = "U5 – Nadpisz stan magazynowy",
            description = "Bezpośrednio ustawia nową wartość quantity dla istniejącego wpisu inventory "
                    + "(warehouse_id + product_id). Aktualizuje updated_at. Zwraca liczbę zmienionych wierszy."
    )
    @PatchMapping("/inventory/{warehouseId}/{productId}")
    public ResponseEntity<Map<String, Object>> updateInventory(
            @PathVariable long warehouseId,
            @PathVariable long productId,
            @RequestBody ShopRequests.UpdateQuantity req) {
        return ResponseEntity.ok(affectedRows(shopService.updateInventory(warehouseId, productId, req.quantity())));
    }

    @Operation(
            summary = "U6 – Anuluj zamówienia opłacone daną metodą",
            description = "Ustawia status = 'CANCELLED' dla wszystkich zamówień, które mają płatność "
                    + "z metodą o podanym kodzie (np. BLIK). Używa JOIN w klauzuli FROM. "
                    + "Zwraca liczbę zmienionych zamówień."
    )
    @PostMapping("/payment-methods/{code}/cancel-orders")
    public ResponseEntity<Map<String, Object>> cancelOrdersByPaymentMethod(
            @Parameter(description = "Kod metody płatności (payment_methods.code)") @PathVariable String code) {
        return ResponseEntity.ok(affectedRows(shopService.cancelOrdersByPaymentMethod(code)));
    }

    // ═══════════════════════════════════════════════════════════════════
    //  DELETE
    // ═══════════════════════════════════════════════════════════════════

    @Operation(
            summary = "D1 – Usuń stare zamówienia klienta",
            description = "Usuwa zamówienia danego klienta utworzone przed cutoffDate. "
                    + "Kaskadowo usuwa powiązane order_items i order_payments (ON DELETE CASCADE). "
                    + "Zwraca liczbę usuniętych zamówień."
    )
    @DeleteMapping("/customers/{customerId}/old-orders")
    public ResponseEntity<Map<String, Object>> deleteOldCustomerOrders(
            @PathVariable long customerId,
            @Parameter(description = "Data graniczna ISO-8601, np. 2025-01-01T00:00:00+01:00")
            @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE_TIME) OffsetDateTime cutoffDate) {
        return ResponseEntity.ok(affectedRows(shopService.deleteOldCustomerOrders(customerId, cutoffDate)));
    }

    @Operation(
            summary = "D2 – Usuń koszyk (zamówienie NEW)",
            description = "Usuwa zamówienie TYLKO jeśli ma status = 'NEW' (warunek bezpieczeństwa). "
                    + "Kaskadowo usuwa powiązane order_items i order_payments. "
                    + "Zwraca liczbę usuniętych wierszy (0 lub 1)."
    )
    @DeleteMapping("/orders/{orderId}")
    public ResponseEntity<Map<String, Object>> deleteCart(
            @PathVariable long orderId) {
        return ResponseEntity.ok(affectedRows(shopService.deleteCart(orderId)));
    }

    @Operation(
            summary = "D3 – Usuń pozycje zamówień produktów danej marki",
            description = "Usuwa z order_items wszystkie pozycje, których product_id należy do produktów "
                    + "danej marki (brand_id). Operacja bulk — może usunąć wiele wierszy. "
                    + "Zwraca liczbę usuniętych pozycji."
    )
    @DeleteMapping("/brands/{brandId}/order-items")
    public ResponseEntity<Map<String, Object>> deleteOrderItemsByBrand(
            @PathVariable long brandId) {
        return ResponseEntity.ok(affectedRows(shopService.deleteOrderItemsByBrand(brandId)));
    }

    @Operation(
            summary = "D4 – Usuń klienta",
            description = "Usuwa klienta po customer_id. UWAGA: operacja się nie powiedzie (FK violation), "
                    + "jeśli klient ma powiązane zamówienia (brak ON DELETE CASCADE na orders.customer_id). "
                    + "Zwraca liczbę usuniętych wierszy."
    )
    @DeleteMapping("/customers/{customerId}")
    public ResponseEntity<Map<String, Object>> deleteCustomer(
            @PathVariable long customerId) {
        return ResponseEntity.ok(affectedRows(shopService.deleteCustomer(customerId)));
    }

    @Operation(
            summary = "D5 – Usuń magazyn",
            description = "Usuwa magazyn po warehouse_id. Kaskadowo usuwa wpisy inventory (ON DELETE CASCADE). "
                    + "Zwraca liczbę usuniętych wierszy."
    )
    @DeleteMapping("/warehouses/{warehouseId}")
    public ResponseEntity<Map<String, Object>> deleteWarehouse(
            @PathVariable long warehouseId) {
        return ResponseEntity.ok(affectedRows(shopService.deleteWarehouse(warehouseId)));
    }

    @Operation(
            summary = "D6 – Usuń pozycje zamówień produktów danej kategorii",
            description = "Usuwa z order_items wszystkie pozycje, których product_id należy do produktów "
                    + "danej kategorii (category_id). Operacja bulk — może usunąć wiele wierszy. "
                    + "Zwraca liczbę usuniętych pozycji."
    )
    @DeleteMapping("/categories/{categoryId}/order-items")
    public ResponseEntity<Map<String, Object>> deleteOrderItemsByCategory(
            @PathVariable long categoryId) {
        return ResponseEntity.ok(affectedRows(shopService.deleteOrderItemsByCategory(categoryId)));
    }
}
